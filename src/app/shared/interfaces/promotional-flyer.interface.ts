export interface IPromotionalflyer {
  id: number;
  name: string;
  createdDate: string;
  status: string;
}

export interface IPromotionalFlyerProducts {
  id: number;
  name: string;
  salePrice?: number;
}
